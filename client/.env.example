# Client Environment Variables
# Copy this file to .env and update values as needed

# API URL - leave empty to use proxy (recommended for development)
# For production, set to your API server URL
# VITE_API_URL=http://localhost:5001/api
# VITE_API_URL=https://your-api-domain.com/api

# Development server port (optional, defaults to 5174)
# VITE_PORT=5174

# API target for proxy (development only, defaults to localhost:5001)
# VITE_API_TARGET=http://localhost:5001
