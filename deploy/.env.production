# =============================================================================
# LAILA Production Environment Configuration
# =============================================================================
# Copy this file to server/.env and fill in all values before deploying.
# Lines marked [REQUIRED] must be set. Others have sensible defaults.
# =============================================================================

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
NODE_ENV=production
PORT=5001

# [REQUIRED] Frontend URL for CORS (no trailing slash)
CLIENT_URL=https://__DOMAIN__

# ---------------------------------------------------------------------------
# Database — PostgreSQL (required for production)
# ---------------------------------------------------------------------------
# [REQUIRED] PostgreSQL connection string
DATABASE_URL="postgresql://__DB_USER__:__DB_PASS__@localhost:5432/__DB_NAME__"

# ---------------------------------------------------------------------------
# Secrets — auto-generated by deploy.sh if left blank
# ---------------------------------------------------------------------------
# [REQUIRED] Generate with: openssl rand -base64 32
JWT_SECRET=
SESSION_SECRET=

# ---------------------------------------------------------------------------
# AI Providers (at least one required for AI features)
# ---------------------------------------------------------------------------

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# For local LLM (LM Studio, Ollama, etc.):
# OPENAI_BASE_URL=http://localhost:1234/v1
# OPENAI_MODEL=local-model

# Google Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-pro

# ---------------------------------------------------------------------------
# Database Seeding (optional — only needed on first deploy)
# ---------------------------------------------------------------------------
# If not set, random secure passwords are generated and logged.
# SEED_ADMIN_PASSWORD=
# SEED_INSTRUCTOR_PASSWORD=
# SEED_STUDENT_PASSWORD=

# ---------------------------------------------------------------------------
# Email (optional — for notification features)
# ---------------------------------------------------------------------------
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com
